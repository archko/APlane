<?xml version="1.0" encoding="utf-8"?>
<!-- Layout used by ImageOptionPreference for the image option style.
     This is inflated inside android.R.layout.preference. 
     -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="1dp"
    android:paddingRight="1dp">

    <SeekBar android:id="@+id/pref_seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView android:id="@+id/label"
        android:layout_below="@id/pref_seek_bar"
        android:text="@string/pref_current" android:textSize="12sp"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_marginTop="-4dp"/>

    <TextView android:id="@+id/pref_seek_current_value"
        android:layout_below="@id/pref_seek_bar" android:layout_toRightOf="@id/label"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_marginTop="-4dp" android:textSize="12sp"/>

</RelativeLayout>